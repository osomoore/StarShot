"""StarShot v2: authenticated online multiplayer built on the core rules engine.

This package adds player accounts, sessions, matchmaking, server-side AI
pilots, and per-viewer redacted game views. Games created here always use the
core_0_3 deck set via a scoped deck-set override, independent of the
process-wide default used by the v1 debug API.
"""
